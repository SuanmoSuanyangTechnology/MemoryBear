你需要把用户画像短句整理成适合用户名片展示的标签。

输入：
{{ input_json }}

要求：
1. 只能依据输入的 interests、traits、goals，不补充输入外信息。
2. 将事实短句归一化为 2～8 个中文字符的词或短语；英文或混合文本也应尽量简短。
3. 合并同义、近义和重复表达。
4. 删除临时询问、一次性意图和信息不足的内容。
5. 优先覆盖多个非空类别，每个类别最多返回 2 个候选。
6. 最多返回 6 个候选，不需要凑满。
7. 按稳定性和用户名片展示价值从高到低排序。
8. 每项返回 name 和 category；category 只能是 interests、traits 或 goals。
9. 只返回一个 JSON 对象，不要返回 Markdown、解释或顶层数组。顶层字段必须是 tags，格式严格如下：
{
  "tags": [
    {"name": "标签名称", "category": "interests"}
  ]
}
10. 没有合适候选时返回 {"tags": []}。
